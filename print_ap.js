const firstTerm = 4
const noTerm = 10
const diff = 3
let currentTermNum = 0
let currentTerm = firstTerm
let apNow = firstTerm
let sum = 0
console.log(currentTerm);
while (currentTermNum < (noTerm-1)) {
    currentTerm = currentTerm + diff
    currentTermNum = currentTermNum + 1
    console.log(currentTerm);

    
}
